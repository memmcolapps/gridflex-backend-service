server:
  port: 9080

app:
  base-path: /${BASE_ADDRESS}
#  /api/v1
#  /grid-flex/v1/api

spring:
  jackson:
    default-property-inclusion: non_null

odyssey:
  jwt-secret: ${JWT_SECRET}

security:
  header:
    key: ${HEADER_KEY}
  admin:
    value: ${ADMIN_API_VALUE}
  user:
    value: ${USER_API_VALUE}
  setup:
    value: ${SETUP_API_VALUE}

#external:
#  token-gen:
#    base-url: http://${T_ADDRESS}:${T_PORT}
#  hes-endpoint:
#    base-url: http://${HES_ADDRESS}:${PORT}/api
#  hes-realtime:
#    base-url: http://${HES_ADDRESS}:${PORT}/api/realtime
#  dlms-write-ops:
#    base-url: http://${HES_ADDRESS}:${PORT}/api/dlms

springdoc:
  api-docs:
    path: /gridflex/api-docs

  swagger-ui:
    path: /swagger-ui.html
    config-url: /${DOC_ADDRESS}/api-docs/swagger-config
    url: /${DOC_ADDRESS}/api-docs

  packages-to-scan: org.memmcol.gridflexbackendservice.thirdPartyService
#    urls:
#      - name: Admin
#        url: /v3/api-docs/admin

management:
  server:
    port: ${server.port}  # Use same port for actuator unless you want a separate port
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    tags:
      application: ${spring.application.name}

resilience4j:
  circuitbreaker:
    instances:
      productService:
        register-health-indicator: true
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s


#  instance:
#    prefer-ip-address: true
#    health-check-url-path: /actuator/health
#    health-check-interval-seconds: 10

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health, info, prometheus

#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#  endpoint:
#    prometheus:
#      enabled: true
#  prometheus:
#    metrics:
#      export:
#        enabled: true



  #  datasource:
  #    hikari:
  #      maximum-pool-size: 10
  #      minimum-idle: 5
  #      idle-timeout: 30000
  #      max-lifetime: 1800000
  #      connection-timeout: 30000
  #      leak-detection-threshold: 20000

  #external:
  #  token-gen:
  #    base-url: http://169.239.189.91:19071
  #  hes-endpoint:
  #    base-url: http://127.0.0.1:9061/api
  #  hes-realtime:
  #    base-url: http://127.0.0.1:9061/api/realtime
  #  dlms-write-ops:
  #    base-url: http://127.0.0.1:9061/api/dlms
  #    base-url: http://192.168.1.76:9061/api


#spring:
#  application:
#    name: authorization-service
#  config:
#    import: optional:configserver:http://localhost:8888
##  cloud:
##    config:
##      uri: http://localhost:8888  # Pointing to your config-service
##  sleuth:
##    sampler:
##      probability: 1.0
##  zipkin:
##    base-url: http://localhost:9411/